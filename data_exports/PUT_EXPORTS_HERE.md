# data_exports/

Place exported files from AnimaLoraStudio here, such as:

- Dataset archives
- Preset / config bundles
- Models or datasets downloaded from ModelScope

The contents of this directory are excluded by `.gitignore` and will not be committed.
